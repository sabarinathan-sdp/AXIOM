# AXIOM
Deterministic policy gate and capability registry for a local, single-user agentic AI system — the LLM proposes, Boolean checks dispose. Default-deny, simulate-before-execute, no scoring in the trust boundary.
